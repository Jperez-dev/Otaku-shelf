# Environment variables for Vercel deployment
# These can be set in your Vercel dashboard under Settings > Environment Variables

# Backend URL for image proxy and API services
VITE_BACKEND_URL=http://localhost:3001

# Production API URLs (if needed in the future)
VITE_API_BASE_URL=https://api.mangadex.org
VITE_COVERS_BASE_URL=https://uploads.mangadex.org/covers

# App configuration
VITE_APP_NAME=Otaku Shelf
VITE_APP_VERSION=1.0.0